<tr>
  <td><img src=<%=group.icon%> alt=<%=group.name%>></td>
  <td><%= link_to "#{group.name}", group_entities_path(group.id)  %></td>
  <td><h3>Amount: $<%="#{group.amount}"%></h3></td>
  <td>
  <%= button_to "Delete this category", group_url(id: group.id), method: :delete %>
  </td>
</tr>
